# assets

Static files imported by the application: images, fonts and icons.

Currently unused: the UI is built from text and inline styles only, and the viewer
is embedded as a remote iframe, so no binary asset is bundled with the host app.
